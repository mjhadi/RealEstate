
{% extends "master.html.twig" %}
{% block title %}Photo list{% endblock %}
{% block content %}
    <p><a href="/news/add">Add photo</a><p>
    <table border=1>
        <tr><th>#</th><th>Image Title</th><th>Picture</th><th>action</th></tr>
        {% for img in list %}
                <tr class="{% if loop.index is odd %}rowodd{% else %}roweven{% endif %}">
                    <td>{{img.imageId}}</td>
                    <td>{{img.imageTitle}}</td>
                    <td><img src="{{img.imagePath}}"  width="150"></td>
                                   
                    <td>
                        <a href="/photo/delete/{{img.imageId}}">Delete</a>
                        <a href="/photo/edit/{{img.imageId}}">Edit</a>
                    </td>
                </tr>
        {% else %}
            <tr><td colspan="6"> You have no images</td></tr>
        {% endfor %}
    </table>    
{% endblock %}

